{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ reservation.total }}</td>
            </tr>
            <tr>
                <th>Enfants</th>
                <td>{{ reservation.enfants }}</td>
            </tr>
            <tr>
                <th>Adultes</th>
                <td>{{ reservation.adultes }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ reservation.dateDebut ? reservation.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ reservation.dateFin ? reservation.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_index') }}" class="btn">retour</a>

    <a href="{{ path('app_reservation_edit', {'id': reservation.id}) }}" class="btn">modifier</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
